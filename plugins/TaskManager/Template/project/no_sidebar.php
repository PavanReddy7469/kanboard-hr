<?php
/* Deliberately empty.

   Our Zoho-shaped pages carry no second sidebar: the left rail handles
   navigation and the project header carries the tab strip, so a third
   column of links only steals width from the grid. Project settings stay
   one click away under "Configure this project" in the project header,
   which is where Zoho puts them too. */
